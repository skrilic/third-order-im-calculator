import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'third.order.im.calculator',
  appName: 'third-order-im-calculator',
  webDir: 'build'
};

export default config;
