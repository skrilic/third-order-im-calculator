import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  // appId is the store and device identity — changing it would publish a
  // different app, so it stays as it is regardless of the display name.
  appId: 'third.order.im.calculator',
  appName: 'Intermod RF Sites',
  webDir: 'build'
};

export default config;
